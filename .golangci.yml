# golangci-lint configuration with Antimoji integration
# Generated by: antimoji setup-lint --mode=zero-tolerance

version: "2"

run:
  timeout: 5m
  go: '1.21'

linters:
  enable:
    - errcheck      # Check for unchecked errors
    - govet         # Report suspicious constructs
    - ineffassign   # Detect ineffectual assignments
    - staticcheck   # Advanced static analysis
    - unused        # Find unused code
    - misspell      # Finds commonly misspelled English words
    - antimoji      # Emoji detection and linting

linters-settings:
  custom:
    antimoji:
      path: antimoji
      description: "Emoji detection and linting"
      original-url: github.com/antimoji/antimoji
      settings:
        config: .antimoji.yaml
        mode: zero-tolerance

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
